//void setup(){}
//void loop(){
//int irRShift = irRValue - rightAverage;
//int irLShift = irLValue - rightAverage;
//
//  //Right LED mapping
//  sensorRValue = analogRead(rightEye);
//  // apply the calibration to the sensor reading
//  sensorRValue = map(sensorRValue, sensorMin, sensorMax, 0, 255);
//  // in case the sensor value is outside the range seen during calibration
//  sensorRValue = constrain(sensorRValue, 0, 255);
//  // fade the LED using the calibrated value:
//  analogWrite(rightLED, sensorRValue);

//  //left LED mapping
//  sensorLValue = analogRead(lefttEye);
//  sensorValue = map(sensorLValue, sensorMin, sensorMax, 0, 255);
//  sensorValue = constrain(sensorLValue, 0, 255);
//  analogWrite(leftLED, sensorLValue);}
